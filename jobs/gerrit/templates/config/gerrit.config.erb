[gerrit]
	basePath = git
	canonicalWebUrl = http://localhost:8080/
[database]
    type = <%= p('gerrit.database.type') %>
    hostname = <%= p('gerrit.database.host') %>
    database = <%= p('gerrit.database.name') %>
    username = <%= p('gerrit.database.username') %>
[index]
	type = LUCENE
[auth]
	type = OPENID
[receive]
	enableSignedPush = false
[sendemail]
	smtpServer = localhost
[container]
	user = vcap
	javaHome = /var/vcap/packages/oraclejdk/jre
[sshd]
	listenAddress = *:29418
[httpd]
	listenUrl = http://*:8080/
[cache]
	directory = cache
