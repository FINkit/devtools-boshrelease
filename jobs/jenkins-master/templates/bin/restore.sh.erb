#!/usr/bin/env bash
set -euxo

JOB_NAME=$1
export JOB_DIR=/var/vcap/jobs/$JOB_NAME
source $JOB_DIR/data/properties.sh

rm -r $JENKINS_HOME/*
tar -C $JENKINS_HOME -xzf $BBR_ARTIFACT_DIRECTORY/jenkins.tgz
