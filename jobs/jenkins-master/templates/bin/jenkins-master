#!/bin/sh -eu

JAVA=${JAVA_HOME}/bin/java

export JAVA_OPTS=""
export JAVA_TOOL_OPTIONS="-Djava.awt.headless=true"

JENKINS_VERSION=2.60.3
JENKINS=/var/vcap/jobs/jenkins-master/packages/jenkins/jenkins/jenkins_${JENKINS_VERSION}.war
MEMORY_SETTINGS=""

export JENKINS_HOME=/var/vcap/store/jenkins-master
JENKINS_ARGS=""

# start jenkins
echo "${JAVA} ${MEMORY_SETTINGS} ${JAVA_OPTS} -jar ${JENKINS} ${JENKINS_ARGS}"
${JAVA} ${MEMORY_SETTINGS} ${JAVA_OPTS} -jar ${JENKINS} ${JENKINS_ARGS}
