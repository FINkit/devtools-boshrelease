#!/usr/bin/env bash

# job template binding variables

# job name & index of this VM within cluster
# e.g. JOB_NAME=redis, JOB_INDEX=0
export NAME='<%= name %>'
export JOB_INDEX=<%= index %>
# full job name, like redis/0 or webapp/3
export JOB_FULL="$NAME/$JOB_INDEX"

# configure custom properties for job
export JAVA_HOME=/var/vcap/packages/oraclejdk
export JENKINS_VERSION=2.60.3
export JAVA=${JAVA_HOME}/bin/java

export JAVA_OPTS=""
export JAVA_TOOL_OPTIONS="-Djava.awt.headless=true"

export JENKINS=/var/vcap/packages/jenkins/jenkins/jenkins_${JENKINS_VERSION}.war
export MEMORY_SETTINGS=""

export JENKINS_HOME=/var/vcap/store/jenkins-master
export JENKINS_ARGS=""

export LD_LIBRARY_PATH=/var/vcap/packages/git/usr/lib/x86_64-linux-gnu:/var/vcap/packages/git/usr/lib/git-core
export INCLUDE_PATH="/var/vcap/packages/git/apt/usr/include:${INCLUDE_PATH:-}"
export CPATH="$INCLUDE_PATH"
export CPPPATH="$INCLUDE_PATH"
